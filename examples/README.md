# Examples

这些文件用于演示审计工具的数据结构，也作为 CI 的最小可运行样例。示例不是完整剧本，不代表唯一正确的导演方案。

- `segments-seedance2.0.json`：14 秒短段
- `segments-seedance2.5.json`：24 秒长段，包含 3 个 microbeats
- `continuity.json`：两个相邻分段的基础人物 / 道具交接
- `quick-prompt.txt`：四模块快速执行版 Prompt
